# Capture the selected policy's group list before adding X301MLKEM1024.
[x301_policy_capture]
base_groups = ${crypto_policy::Groups}

[crypto_policy]
Groups = *?X301MLKEM1024/${x301_policy_capture::base_groups}
