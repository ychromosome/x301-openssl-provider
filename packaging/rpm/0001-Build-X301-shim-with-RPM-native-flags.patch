diff --git a/provider/Cargo.lock b/provider/Cargo.lock
index 6b5c2cf..9c3fa39 100644
--- a/provider/Cargo.lock
+++ b/provider/Cargo.lock
@@ -199,6 +199,7 @@ checksum = "b6f5e870be6c3b371b77fe0ee0bafb859fa4964b4404c27de1d380043c4dda20"
 name = "x301-provider"
 version = "0.1.0"
 dependencies = [
+ "cc",
  "crypto-bigint",
  "ed301-eddsa",
  "zeroize",
diff --git a/provider/crates/x301-provider/Cargo.toml b/provider/crates/x301-provider/Cargo.toml
index 1d59be4..6ad6243 100644
--- a/provider/crates/x301-provider/Cargo.toml
+++ b/provider/crates/x301-provider/Cargo.toml
@@ -28,3 +28,6 @@ tls-x301-mlkem1024 = []
 crypto-bigint = { version = "=0.7.5", default-features = false }
 ed301-eddsa = { path = "../../../crates/ed301-eddsa", features = ["x301"] }
 zeroize = { version = "=1.9.0", default-features = false }
+
+[build-dependencies]
+cc = { version = "=1.2.66", default-features = false }
diff --git a/provider/crates/x301-provider/build.rs b/provider/crates/x301-provider/build.rs
index f14ee1a..8d9575c 100644
--- a/provider/crates/x301-provider/build.rs
+++ b/provider/crates/x301-provider/build.rs
@@ -6,7 +6,6 @@
 use std::env;
 use std::fs;
 use std::path::PathBuf;
-use std::process::Command;
 
 fn require_exact_environment(name: &str, expected: &str) {
     let actual = env::var(name).unwrap_or_else(|_| panic!("{name} is required"));
@@ -32,7 +31,7 @@ fn canonical_directory(name: &str) -> PathBuf {
     path
 }
 
-fn reject_native_injection_environment() {
+fn reject_native_injection_environment(allow_package_build_flags: bool) {
     const EXACT: &[&str] = &[
         "ARFLAGS",
         "CFLAGS",
@@ -56,6 +55,7 @@ fn reject_native_injection_environment() {
         "TARGET_ARFLAGS",
         "TARGET_CFLAGS",
     ];
+    const PACKAGE_BUILD_FLAGS: &[&str] = &["CFLAGS", "CXXFLAGS", "LDFLAGS"];
 
     for (name, _) in env::vars_os() {
         let Some(name) = name.to_str() else {
@@ -73,8 +73,10 @@ fn reject_native_injection_environment() {
         ]
         .iter()
         .any(|prefix| name.starts_with(prefix));
+        let forbidden_exact = EXACT.contains(&name)
+            && !(allow_package_build_flags && PACKAGE_BUILD_FLAGS.contains(&name));
         assert!(
-            !prefixed && !EXACT.contains(&name),
+            !prefixed && !forbidden_exact,
             "native build override is forbidden: {name}"
         );
     }
@@ -91,6 +93,7 @@ fn main() {
         "OPENSSL_LIB_DIR",
         "CC",
         "AR",
+        "X301_ALLOW_PACKAGE_BUILD_FLAGS",
     ] {
         println!("cargo:rerun-if-env-changed={name}");
     }
@@ -104,111 +107,51 @@ fn main() {
     };
     require_exact_environment("CC", compiler_path);
     require_exact_environment("AR", "/usr/bin/ar");
-    reject_native_injection_environment();
+    let allow_package_build_flags = env::var_os("X301_ALLOW_PACKAGE_BUILD_FLAGS").is_some();
+    if allow_package_build_flags {
+        require_exact_environment("X301_ALLOW_PACKAGE_BUILD_FLAGS", "1");
+    }
+    reject_native_injection_environment(allow_package_build_flags);
     let include_dir = canonical_directory("OPENSSL_INCLUDE_DIR");
     let lib_dir = canonical_directory("OPENSSL_LIB_DIR");
 
-    let output_dir = canonical_directory("OUT_DIR");
-    let object = output_dir.join("provider_shim.o");
-    let hybrid_object = output_dir.join("hybrid_kem.o");
-    let archive = output_dir.join("libx301_provider_shim.a");
+    let _output_dir = canonical_directory("OUT_DIR");
     let hybrid = env::var_os("CARGO_FEATURE_TLS_X301_MLKEM1024").is_some();
     let sanitizer = env::var_os("CARGO_FEATURE_TEST_SANITIZER").is_some();
     assert!(
         !(sanitizer && fuzz_coverage),
         "sanitizer and fuzz-coverage variants must remain separate"
     );
-    let mut compiler = Command::new(compiler_path);
-    compiler
-        .arg("-std=c11")
-        .arg("-fPIC")
-        .arg("-fvisibility=hidden")
-        .arg("-fstack-protector-strong")
-        .arg("-Wall")
-        .arg("-Wextra")
-        .arg("-Werror")
-        .arg("-I")
-        .arg(&include_dir)
-        .arg("-c")
-        .arg("c/provider_shim.c")
-        .arg("-o")
-        .arg(&object);
+    let mut build = cc::Build::new();
+    build
+        .compiler(compiler_path)
+        .archiver("/usr/bin/ar")
+        .include(&include_dir)
+        .file("c/provider_shim.c")
+        .std("c11")
+        .pic(true)
+        .flag_if_supported("-fvisibility=hidden")
+        .flag_if_supported("-fstack-protector-strong")
+        .warnings(true)
+        .warnings_into_errors(true);
     if sanitizer {
-        compiler
-            .arg("-fsanitize=address,undefined")
-            .arg("-fno-sanitize-recover=all")
-            .arg("-fno-omit-frame-pointer");
+        build
+            .flag("-fsanitize=address,undefined")
+            .flag("-fno-sanitize-recover=all")
+            .flag("-fno-omit-frame-pointer");
     }
     if fuzz_coverage {
-        compiler.arg("-fsanitize-coverage=inline-8bit-counters,pc-table,trace-cmp");
+        build.flag("-fsanitize-coverage=inline-8bit-counters,pc-table,trace-cmp");
     }
     if env::var_os("CARGO_FEATURE_TEST_FAILPOINT").is_some() {
-        compiler.arg("-DX301_TEST_FAILPOINT_ARTIFACT=1");
-    }
-    if hybrid {
-        compiler.arg("-DX301_ENABLE_HYBRID_MLKEM1024=1");
-    }
-    assert!(
-        compiler
-            .status()
-            .expect("failed to execute /usr/bin/gcc")
-            .success(),
-        "C provider shim compilation failed"
-    );
-    if hybrid {
-        let mut hybrid_compiler = Command::new(compiler_path);
-        hybrid_compiler
-            .arg("-std=c11")
-            .arg("-fPIC")
-            .arg("-fvisibility=hidden")
-            .arg("-fstack-protector-strong")
-            .arg("-Wall")
-            .arg("-Wextra")
-            .arg("-Werror")
-            .arg("-DX301_ENABLE_HYBRID_MLKEM1024=1")
-            .arg("-I")
-            .arg(&include_dir)
-            .arg("-c")
-            .arg("c/hybrid_kem.c")
-            .arg("-o")
-            .arg(&hybrid_object);
-        if sanitizer {
-            hybrid_compiler
-                .arg("-fsanitize=address,undefined")
-                .arg("-fno-sanitize-recover=all")
-                .arg("-fno-omit-frame-pointer");
-        }
-        if fuzz_coverage {
-            hybrid_compiler.arg("-fsanitize-coverage=inline-8bit-counters,pc-table,trace-cmp");
-        }
-        assert!(
-            hybrid_compiler
-                .status()
-                .expect("failed to execute /usr/bin/gcc")
-                .success(),
-            "C hybrid KEM compilation failed"
-        );
+        build.define("X301_TEST_FAILPOINT_ARTIFACT", "1");
     }
-    let mut archiver = Command::new("/usr/bin/ar");
-    archiver.arg("crs").arg(&archive).arg(&object);
     if hybrid {
-        archiver.arg(&hybrid_object);
+        build
+            .define("X301_ENABLE_HYBRID_MLKEM1024", "1")
+            .file("c/hybrid_kem.c");
     }
-    assert!(
-        archiver
-            .status()
-            .expect("failed to execute /usr/bin/ar")
-            .success(),
-        "C provider shim archive failed"
-    );
-
-    println!(
-        "cargo:rustc-link-search=native={}",
-        output_dir
-            .to_str()
-            .expect("canonical OUT_DIR must remain UTF-8")
-    );
-    println!("cargo:rustc-link-lib=static=x301_provider_shim");
+    build.compile("x301_provider_shim");
     if sanitizer {
         /*
          * Test-only native instrumentation.  Stable Rust does not expose a
